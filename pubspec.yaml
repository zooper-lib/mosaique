name: mosaique
description: A declarative, route-driven region-based view composition package for Flutter.
version: 0.1.0
repository: https://github.com/Zooper/mosaique

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
